import dotenv from 'dotenv';
dotenv.config({ path: `.env.production` });
// TODO: this is a sample test to verify if jest is working. Delete it
describe('sample test', () => {
  it('should do something when some situation', () => {
    expect(true).toBeTruthy();
  });
});
